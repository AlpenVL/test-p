pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'Hello from Jenkins!'
                sh 'ls -la'  // Пример команды для Linux
            }
        }
    }
}
